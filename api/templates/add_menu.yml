3Fs apiv2 add food items to menu
---
tags:
 - Add a New food item to Menu
parameters:
  - name: Authorization
    in: header
    type: string
    description: JWT token authorization
    required: true
    example: "Bearer" 
  - in: body
    name: body
    required: true
    type: string
    description: This route is used to add a new food item to Menu.
security:
  - JWT:
      name: Authorization
      in: header
      type: string
      description: JWT token authorization
      required: true
    example: "Bearer" 
responses:
  201:
    description: Successful Add a new food item. Item is added to the database.
    schema:
      id: menu_id
      properties:
        dish_name:
          type: string
        dish_price:
          type: int
        dish_toppings:
          type: string
  400:
    description: Empty dish_name, dish_price and dish_toppings field.