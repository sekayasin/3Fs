3Fs apiv2 Update Order Status
---
tags:
 - Update Order Status
parameters:
  - name: Authorization
    in: header
    type: string
    description: JWT token authorization
    required: true
    example: "Bearer" 
  - in: body
    name: body
    required: true
    type: string
    description: This route is used to update the status of the order.
security:
  - JWT:
      name: Authorization
      in: header
      type: string
      description: JWT token authorization
      required: true
    example: "Bearer" 
responses:
  201:
    description: Successful. An Order is updated to either (Processing, Cancelled, Completed)
    schema:
      id: update_order
      properties:
        order_status:
          type: string
  400:
    description: Empty order_status field.